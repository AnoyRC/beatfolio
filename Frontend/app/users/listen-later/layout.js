export const metadata = {
  title: 'Beatfolio | Listen Later',
  description:
    'A decentralized social media platform tailored exclusively for artists, musicians, and creators.',
};

export default function LaterLayout({ children }) {
  return <>{children}</>;
}
